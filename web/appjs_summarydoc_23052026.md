| Area                 | Function                        | Summary                                                                                                           | Side effects / notes                                                                         |
| -------------------- | ------------------------------- | ----------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| Basic helpers        | `$`                             | Returns DOM element by ID.                                                                                        | Central DOM helper used everywhere.                                                          |
| Basic helpers        | `fmt`                           | Converts empty/null/undefined values to `--`; otherwise stringifies.                                              | Used for table/meta display.                                                                 |
| Basic helpers        | `compact`                       | Formats numbers with US thousands separators.                                                                     | Used in UI counters.                                                                         |
| Basic helpers        | `truncate`                      | Shortens long strings with `...`.                                                                                 | Used for graph labels and tooltips.                                                          |
| API                  | `api`                           | Wraps `fetch`, parses JSON/text safely, logs browser-side API call metadata.                                      | Updates `state.runtimeLogs`; throws on HTTP/payload failure.                                 |
| Boot                 | `boot`                          | Initializes drawer, graph, actions, camera widget, then loads data.                                               | Entry point called at file end.                                                              |
| Boot                 | `loadAll`                       | Loads summary, workflow graph, analytics, first trace events, and health.                                         | Sets graph/procedure/runtime state, default focus, renders whole UI, loads runs.             |
| Boot                 | `setConnection`                 | Updates top-left connection state pill.                                                                           | Mutates DOM class/text.                                                                      |
| Focus                | `resolveDefaultFocus`           | Chooses initial graph focus from backend `default_focus` or dominant SP.                                          | Falls back to first top cached SP.                                                           |
| Focus                | `resolveNodeIdFromName`         | Resolves a procedure name to a graph node ID.                                                                     | Case-insensitive fallback.                                                                   |
| Scope                | `graphScopeVisibleIds`          | Returns node IDs visible for current graph scope: all, focused, selected, or neighbourhood.                       | Uses backend focus hint when available.                                                      |
| Scope                | `neighbourhoodIds`              | Expands a seed set through graph edges and child steps up to a depth.                                             | Used for focused/selected/neighbourhood scopes.                                              |
| Edges                | `edgePriority`                  | Assigns numeric priority by edge kind.                                                                            | Calls/errors/writes rank higher than reads/static steps.                                     |
| Edges                | `shouldShowEdge`                | Decides whether an edge should render for the current scope.                                                      | Hides lower-priority edges in focused mode.                                                  |
| Labels               | `shouldShowLabel`               | Decides whether a node gets a visible sprite label.                                                               | Prioritizes selected neighbourhood, focus, errors, damage, important nodes, active evidence. |
| Labels               | `nodeMatchesActiveEvidence`     | Checks whether a step node matches current events/run evidence.                                                   | Uses `state.activeRun ? state.runEvents : state.events`.                                     |
| Labels               | `nodeDisplayName`               | Builds user-friendly node display text.                                                                           | Cleans SP/table names and prefixes step numbers.                                             |
| Labels               | `nodeLabelMeta`                 | Builds secondary label metadata.                                                                                  | Includes lane, type, row count, or step/table classification.                                |
| Labels               | `topTypeForNode`                | Finds dominant trace type for a node.                                                                             | Sorts `node.types` descending.                                                               |
| Labels               | `cleanObjectName`               | Removes technical prefixes/suffixes and converts underscores to spaces.                                           | Improves readability of SP/table labels.                                                     |
| Labels               | `isMeaningfulTableName`         | Filters generic/useless table names.                                                                              | Used to avoid noisy table labels.                                                            |
| Summary              | `renderSummary`                 | Renders metrics, dominant SP/type, watermark, runtime start, date min/max.                                        | Also configures min/max for date inputs.                                                     |
| Filters              | `renderFilters`                 | Populates SP and type dropdowns from cached summary.                                                              | Uses cleaned display labels but keeps real values.                                           |
| Filters              | `fillSelect`                    | Rebuilds a `<select>` with option objects or strings.                                                             | Preserves current value when possible.                                                       |
| Date helpers         | `toDateTimeLocal`               | Converts SQL-ish datetime to `datetime-local` style text.                                                         | Returns first 16 chars.                                                                      |
| Date helpers         | `normalizeDateFilter`           | Normalizes date inputs to SQL-friendly `YYYY-MM-DD HH:mm:ss`.                                                     | Accepts date-only and minute-level inputs.                                                   |
| Date helpers         | `validateDateRange`             | Validates From/To format and order.                                                                               | Returns error message or empty string.                                                       |
| Context              | `hasActiveFilterContext`        | Checks whether any meaningful filter/run context exists.                                                          | Used for scoped diagnosis.                                                                   |
| Context              | `expectedContextName`           | Returns active run name or selected filter SP.                                                                    | Expected-path context source.                                                                |
| Context              | `syncExpectedPathContext`       | Rebuilds or clears expected path context.                                                                         | Calls `buildExpectedPathFromGraph`.                                                          |
| Drawer               | `setupDrawer`                   | Wires drawer tab buttons and collapse toggle.                                                                     | Mutates drawer mode/collapsed state.                                                         |
| Actions              | `bindActions`                   | Wires all UI controls: sync, filters, run selector, graph controls, modals, wrong mode, playback controls.        | Large function; most UI behavior starts here.                                                |
| Actions nested       | `activateWrongMode`             | Enables scoped damage mode, opens diagnosis drawer, loads scoped playback if needed.                              | Sets graph scope to all and renders analytics/graph.                                         |
| Playback controls    | `stopPlayback`                  | Stops interval playback and clears selected node.                                                                 | Resets playback index/status and rerenders graph.                                            |
| Filters              | `applyFilters`                  | Reads filters from DOM, validates dates, clears selected run/playback, adjusts graph focus, reloads events/runs.  | Handles stale focus clearing when SP filter is removed.                                      |
| Events               | `loadFilteredEvents`            | Calls `/api/trace/events` with current filters and renders results.                                               | Updates `state.events`, connection status, graph.                                            |
| Runs                 | `loadRuns`                      | Calls `/api/runs` for current SP/type/time filters.                                                               | Populates run selector; shows fallback if route unavailable.                                 |
| Runs                 | `renderRunOptions`              | Renders reconstructed run dropdown options.                                                                       | Displays start time, cleaned SP, type, status, event count.                                  |
| Playback             | `playbackForCurrentContext`     | Builds `/api/playback` query from selected run or active filters.                                                 | Includes run ID when selected.                                                               |
| Damage/playback      | `ensureScopedPlaybackForDamage` | Ensures playback delta exists for selected run or filtered SP before diagnosis.                                   | Used by “What is wrong?” mode.                                                               |
| Runs                 | `loadSelectedRun`               | Loads `/api/runs/{runId}`, makes run active, loads playback delta, focuses graph on run SP.                       | Sets `state.events = state.runEvents`; graph remains SP context.                             |
| Time helpers         | `shortTime`                     | Formats timestamp to `MM-DD HH:mm` style slice.                                                                   | Used in run labels.                                                                          |
| Time helpers         | `shortRuntimeTime`              | Formats runtime start timestamp for display.                                                                      | Falls back to raw string slice if invalid.                                                   |
| Filters              | `setFilterStatus`               | Updates filter status message and class.                                                                          | No-op if element missing.                                                                    |
| Expected path        | `buildExpectedPathFromGraph`    | Builds expected-path subgraph and ordered steps for one SP from static graph nodes/edges.                         | Expands related static steps/calls/reads/writes/updates/errors.                              |
| Three.js cleanup     | `clearGroup`                    | Removes and disposes all objects from a Three.js group.                                                           | Prevents graph memory leaks on rerender.                                                     |
| Three.js cleanup     | `disposeObject`                 | Recursively disposes geometry/materials/children.                                                                 | Used by `clearGroup`.                                                                        |
| Three.js cleanup     | `disposeMaterial`               | Disposes material and common texture maps.                                                                        | Defensive cleanup.                                                                           |
| Graph setup          | `setupGraph`                    | Initializes renderer, scene, camera, raycaster, lights, canvas events, resize observer, animation loop.           | Main Three.js initialization.                                                                |
| Selection            | `getSelectionNeighborhood`      | Computes transitive node set to highlight around selected node.                                                   | Includes root SP, child steps, direct connections, connected tables/procs.                   |
| Graph render         | `renderGraph`                   | Full graph renderer: clears scene, chooses nodes, positions them, renders meshes/labels/edges/lane bands/notices. | Updates visible node/edge counts and overlay.                                                |
| Graph filter         | `filteredNodes`                 | Applies graph scope, SP/type/search/step filters, and damage mode filtering to graph nodes.                       | Combines static graph scope with active UI filters.                                          |
| Edges                | `bundleEdges`                   | Merges duplicate edges by source/target/kind and accumulates weight/count.                                        | Reduces visual clutter.                                                                      |
| Graph lanes          | `createLaneBands`               | Adds floor bands and lane labels for workflow/steps/damage/expected modes.                                        | Uses different lane configs per view mode.                                                   |
| Positioning          | `computeNodePositions`          | Computes 3D positions for all visible nodes according to view mode.                                               | Very large function; contains expected, steps, damage, and workflow layout logic.            |
| Positioning nested   | `isProcedureDamaged`            | Inside damage layout, classifies procedure as damaged.                                                            | Uses anomaly names and node kind.                                                            |
| Positioning nested   | `placeNode`                     | Inside workflow layout, places a node near target while avoiding collisions.                                      | Uses bounding-box-like spacing search.                                                       |
| Positioning legacy   | `positionForNode`               | Deprecated stub returning `(0,0,0)`.                                                                              | Not used by current renderer.                                                                |
| Positioning          | `hashNumber`                    | Produces deterministic unsigned hash from a string.                                                               | Used for stable layout offsets.                                                              |
| Graph relation       | `relatedNodeIds`                | Returns root, directly connected nodes, and child steps.                                                          | Used by graph filters.                                                                       |
| Set helper           | `intersectSets`                 | Returns intersection of two sets.                                                                                 | Used by filter narrowing.                                                                    |
| Edges legacy         | `filteredEdges`                 | Filters graph edges to those whose endpoints exist in a node map.                                                 | Appears unused in current render path.                                                       |
| Lanes                | `laneKey`                       | Maps node kind/name to semantic lane.                                                                             | SAP nodes detected by ID containing `SAP`.                                                   |
| Lanes                | `laneMap`                       | Returns fixed lane order mapping.                                                                                 | Ignores input parameter intentionally.                                                       |
| Positioning          | `depthFor`                      | Computes rough graph depth by node kind.                                                                          | Used in workflow positioning.                                                                |
| Color                | `colorFor`                      | Chooses node color based on expected mode, selection, focus, filter, kind.                                        | Has expected-path missing/present coloring branch.                                           |
| Mesh                 | `nodeMesh`                      | Creates Three.js mesh/material/outline for a graph node.                                                          | Dims nodes outside selection neighbourhood.                                                  |
| Mesh                 | `geometryForNode`               | Chooses geometry shape by node kind.                                                                              | Workflow cylinder, error cone, tables slabs/cylinders, unknown octahedron.                   |
| Selection visual     | `applySelectedVisual`           | Adds selected-node scale, emissive color, outline ring, base ring, accent line.                                   | Called during graph render.                                                                  |
| Edges                | `edgeLine`                      | Creates a Three.js line with semantic color/opacity.                                                              | Dims unrelated edges when a node is selected.                                                |
| Labels               | `makeLabel`                     | Creates canvas texture sprite label with title/meta.                                                              | Each label creates a canvas texture.                                                         |
| Canvas helper        | `roundRect`                     | Draws rounded rectangle path on canvas context.                                                                   | Used by `makeLabel`.                                                                         |
| Pointer              | `selectGraphNode`               | Raycasts click target, selects node or clears selection, switches drawer to evidence.                             | Calls `populateDrawerNodeEvidence`.                                                          |
| Pointer              | `onCanvasDoubleClick`           | Raycasts double-click target, centers camera on node, opens inspector.                                            | Calls `inspectNode`.                                                                         |
| Evidence             | `populateDrawerNodeEvidence`    | Loads and displays node evidence in drawer based on node kind.                                                    | May call `/api/workflows/{id}` for procedure nodes.                                          |
| Hover                | `handleHover`                   | Raycasts hover target, shows tooltip, updates hover label state.                                                  | Rerenders graph when hovered node changes.                                                   |
| Inspector            | `inspectNode`                   | Opens modal inspector and displays detailed node evidence.                                                        | Similar to drawer evidence but modal-based.                                                  |
| Inspector helper     | `shapeName`                     | Maps node kind to human-readable shape name.                                                                      | Used in metadata.                                                                            |
| Graph relation       | `incidentEdges`                 | Returns all graph edges connected to an ID.                                                                       | Used for table evidence.                                                                     |
| Graph relation       | `relatedProcedureIds`           | Returns related procedure/workflow/error IDs connected to a node.                                                 | Used in table evidence.                                                                      |
| Modal                | `openModal`                     | Sets modal kicker and reveals modal.                                                                              | Mutates `hidden`.                                                                            |
| Modal                | `closeModal`                    | Hides modal.                                                                                                      | Simple DOM mutation.                                                                         |
| Modal                | `openMapKeyModal`               | Opens graph legend/key modal with colors, shapes, edges, lanes, axes.                                             | Inserts HTML into evidence area.                                                             |
| Modal helper         | `kvHtml`                        | Converts object to key/value HTML.                                                                                | Escapes keys/values.                                                                         |
| Camera               | `resizeGraph`                   | Resizes renderer and updates camera aspect.                                                                       | Uses `.graph-frame` dimensions.                                                              |
| Camera               | `animate`                       | RequestAnimationFrame loop rendering scene.                                                                       | Runs continuously.                                                                           |
| Pointer/camera       | `onPointerDown`                 | Starts drag state for orbit/pan/select.                                                                           | Sets cursor and pointer capture.                                                             |
| Pointer/camera       | `onPointerMove`                 | Handles hover when idle, pan/orbit when dragging.                                                                 | Updates camera continuously.                                                                 |
| Pointer/camera       | `onPointerUp`                   | Ends drag, selects node if click did not move.                                                                    | Updates cursor based on raycast hit.                                                         |
| Camera               | `onWheel`                       | Zooms camera radius.                                                                                              | Prevents default scroll.                                                                     |
| Camera               | `resetCamera`                   | Centers camera on selected node, focused node, or default origin.                                                 | Uses current theta/phi when possible.                                                        |
| Camera               | `setCamera`                     | Sets spherical camera values and updates camera.                                                                  | Used by view buttons and reset.                                                              |
| Camera               | `updateCamera`                  | Converts spherical camera state to position and lookAt target.                                                    | Updates camera matrix.                                                                       |
| Math helper          | `clamp`                         | Restricts numeric value between min/max.                                                                          | Used for camera bounds.                                                                      |
| Overlay              | `renderFilterStatus`            | Updates graph overlay title/info from scope, focus, run, filters, visible counts.                                 | Contains unreachable legacy code after an early `return`.                                    |
| Diagnosis            | `scopedDiagnosisList`           | Returns global or scoped diagnosis list based on wrong mode and filters/run.                                      | Prepends playback delta diagnosis when relevant.                                             |
| Diagnosis            | `playbackDeltaDiagnosis`        | Converts playback delta into a diagnosis card.                                                                    | Handles expected-path unavailable, missing, unexpected rows.                                 |
| Diagnosis            | `diagnosisMatchesScope`         | Checks whether a diagnosis matches active filters.                                                                | Uses evidence text, name/type, SrNo/SubSeqNo, step/q, time window.                           |
| Diagnosis render     | `renderAnalytics`               | Renders damage count and diagnosis cards.                                                                         | Also renders playback/runtime.                                                               |
| Diagnosis focus      | `focusDiagnosis`                | Focuses graph/camera/evidence on selected diagnosis.                                                              | Changes graph scope to focused.                                                              |
| Events render        | `renderEvents`                  | Renders raw trace table.                                                                                          | Uses current `state.events`.                                                                 |
| Runtime render       | `renderRuntime`                 | Renders browser/runtime request logs.                                                                             | Writes into `tab-runtime`.                                                                   |
| Runtime modal        | `openRuntimeModal`              | Loads `/api/runtime/logs`, merges server/browser logs, opens runtime modal.                                       | Updates `state.runtimeStartedAt`.                                                            |
| SP map modal         | `openSpMapModal`                | Opens modal showing parsed stored-procedure knowledge.                                                            | Displays procedure calls/reads/inserts/updates/steps.                                        |
| Export               | `exportGraphImage`              | Exports current Three.js canvas as PNG.                                                                           | Uses `preserveDrawingBuffer`.                                                                |
| Playback             | `runPlayback`                   | Runs playback for current expected context or selected run, stores playback data, opens timeline drawer.          | Shows instruction if no SP/type/run context.                                                 |
| Playback render      | `renderPlayback`                | Renders expected/actual playback rows, delta summary, and evidence JSON.                                          | Shows expected unavailable warning when present.                                             |
| Table helper         | `table`                         | Renders HTML table from columns and rows.                                                                         | Escapes content and truncates cells to 500 chars.                                            |
| Heatmap legacy       | `heatmapHtml`                   | Renders a grid of missing-step heatmap items.                                                                     | Appears unused in current file.                                                              |
| Security helper      | `escapeHtml`                    | Escapes HTML special chars.                                                                                       | Used before inserting user/data values into HTML.                                            |
| Camera widget        | `setupCameraWidget`             | Wires camera pan/orbit/zoom widget buttons.                                                                       | Uses repeated pointer-hold actions.                                                          |
| Camera widget nested | `bindCameraWidgetButton`        | Binds press-and-hold behavior to one camera widget button.                                                        | Starts interval on pointerdown, stops on pointerup/leave/window pointerup.                   |
| Camera widget nested | `start`                         | Starts repeated camera widget action.                                                                             | Nested inside `bindCameraWidgetButton`.                                                      |
| Camera widget nested | `stop`                          | Stops repeated camera widget action.                                                                              | Nested inside `bindCameraWidgetButton`.                                                      |
