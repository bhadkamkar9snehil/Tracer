| Phase                               | File                                                     | Aspect                    | Change Broad Info                                   | Details                                                                                                                                                                                                                                                                                                          |           |                                                                                                                                                                                |
| ----------------------------------- | -------------------------------------------------------- | ------------------------- | --------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Phase 1 — Graph readability         | `web/app.js`                                             | Global graph state        | Add graph focus state                               | Extend `state` with `graphScope`, `focusedNodeId`, `hoveredNodeId`, `showLabels`, `edgeVisibility`, `activeLane`, and `damageOnly`. Current state only has `viewMode`, `wrongMode`, filters, selected node, and playback, so graph readability has no persistent focus model.                                    |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/app.js`                                             | Startup focus             | Default graph to dominant SP                        | After `loadAll()` receives summary/workflows/analytics/events, set `focusedNodeId` to `summary.cache.top_names[0].name`; currently the app loads all graph data and immediately renders the entire graph.                                                                                                        |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/app.js`                                             | `filteredNodes()`         | Replace all-node default with focused-scope default | Change current default `visibleIds = new Set(graph.nodes.map(...))` to a focused set based on dominant SP, selected SP, neighbourhood, or all-system scope. Current implementation starts by making every graph node visible unless filters reduce it.                                                           |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/app.js`                                             | `relatedNodeIds()`        | Add multi-hop graph neighbourhood                   | Replace one-hop only relationship expansion with configurable depth: direct, neighbourhood depth 2, all. Include parent/child step nodes, calls, table edges, error edges, and unknown trace nodes. Current logic only adds direct edge neighbours and child steps.                                              |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/app.js`                                             | `filteredEdges()`         | Add edge filtering and edge priority                | Current `filteredEdges()` returns every edge whose endpoints are visible. Replace with priority rules: always show selected/focused edges, show calls/writes/updates, fade reads, hide static step edges unless selected/focused, hide weak edges in focused mode.                                               |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/app.js`                                             | Edge rendering            | Bundle repeated edges                               | Add edge aggregation before rendering: group by `source + target + kind`, store `count`, `weight`, `kinds`, and draw one line with width/opacity based on weight. Current edge rendering draws one raw line per edge.                                                                                            |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/app.js`                                             | `edgeLine()`              | Add semantic edge styles                            | Replace single `LineBasicMaterial` with semantic styling: call = solid light line, read = thin grey, write/update = thick amber, error = red, static step = faint, predicted = translucent, actual runtime = animated. Current color logic only separates writes/updates/calls/static step.                      |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/app.js`                                             | `laneKey()`               | Stabilize lane order                                | Current lane order is derived from visible nodes. Replace with fixed ordered lanes: Procedure, Workflow/API, Internal Step, Table/Entity, SAP/API/Error, Unknown. Current lane mapping can shift when filters change.                                                                                            |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/app.js`                                             | Lane rendering            | Add visual swimlane bands                           | Replace only floor labels with lane floor rectangles/bands using transparent `PlaneGeometry`, lane separators, and lane headers. Current lanes are only label sprites placed at floor level.                                                                                                                     |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/app.js`                                             | `positionForNode()`       | Create deterministic semantic layout                | Replace current mostly lane/index/depth layout with per-kind lane positioning: focused SP centered, direct calls above/behind, steps in ordered rail, tables right/low, errors left/high, unknowns isolated. Current workflow layout uses lane index, volume, depth, and modulo index.                           |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/app.js`                                             | Labels                    | Hide most labels by default                         | Replace current label condition with `shouldShowLabel(node)`: selected, hovered, focused, high severity, high volume, active playback item. Current logic labels every non-step node, which causes label clutter.                                                                                                |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/app.js`                                             | `makeLabel()`             | Add label categories                                | Split labels into compact graph labels, lane labels, damage labels, hover labels. Use smaller sprites for graph labels, higher-contrast selected labels, and no opaque black card for every node. Current `makeLabel()` always creates the same 512x96 label card.                                               |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/app.js`                                             | Node shapes               | Strengthen object-type encoding                     | Keep existing geometry mapping but refine table read/update/write distinction: read = flat slab, update = rotated diamond slab, write = hex cylinder/slab, actual event = small sphere/bead, predicted = ghost node. Current mapping already separates workflow, error, step, table write/update/read, unknown.  |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/app.js`                                             | `applySelectedVisual()`   | Replace oversized selection beacon                  | Remove the tall translucent blue box. Replace with thin outline, base ring, subtle vertical marker, and selected label. Current selected node adds two edge boxes plus a 160-unit beacon, visually dominating the graph.                                                                                         |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/app.js`                                             | Hover interaction         | Add hover affordance without opening modal          | Add pointer hover raycast separate from click: update `hoveredNodeId`, show temporary label, highlight incident edges, do not open inspector. Current click raycast immediately selects and opens inspector.                                                                                                     |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/app.js`                                             | Graph status overlay      | Show investigation context, not axis explanation    | Change overlay from “X: lane                                                                                                                                                                                                                                                                                     | Y: volume | Z: internal depth” to focused SP, graph scope, visible nodes/edges, hidden nodes, live/cache source, and damage count. Current overlay only shows filter text and node count.  |
| Phase 1 — Graph readability         | `web/index.html`                                         | Graph controls            | Add graph scope selector                            | Add `<select id="graph-scope">Focused SP / Selected SP / Neighbourhood / All System</select>` beside `view-mode`. Current controls only include view mode, reset/top/front/export/playback.                                                                                                                      |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/index.html`                                         | Legend                    | Move permanent legend out of left rail              | Remove permanent Legend/Shapes section from the left rail and add a compact `Map key` button near graph controls or overlay. Current legend occupies a full left-rail panel.                                                                                                                                     |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/styles.css`                                         | Workspace sizing          | Give graph more height                              | Change `.workspace` grid rows so the graph gets more vertical space and bottom panel becomes collapsible/minimized. Current layout fixes bottom area at 280px.                                                                                                                                                   |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/styles.css`                                         | Graph controls            | Improve dense control layout                        | Add wrapping, compact segmented buttons, `graph-scope` width, and smaller instructional text. Current `.graph-controls` is one long flex row.                                                                                                                                                                    |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `web/styles.css`                                         | Graph overlay             | Add focused-map styling                             | Add classes for focus badge, node count, edge count, hidden count, damage count, and mode badge. Current overlay is generic bordered text.                                                                                                                                                                       |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `tracer/analyzer.py`                                     | `graph_payload()`         | Add display metadata to nodes                       | Add `display_label`, `short_label`, `importance`, `severity`, `lane`, `rank`, `degree`, `is_dominant`, and `is_damage`. Current nodes mostly include id, label, kind, shape, trace_count, log_ref_count, step_count, types, and found.                                                                           |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `tracer/analyzer.py`                                     | `graph_payload()`         | Add edge metadata                                   | Add `edge_id`, `label`, `direction`, `priority`, `style`, `count`, `confidence`, and `evidence_type` to edges. Current edges only hold `source`, `target`, `kind`, and `weight`.                                                                                                                                 |           |                                                                                                                                                                                |
| Phase 1 — Graph readability         | `tracer/analyzer.py`                                     | `graph_payload()`         | Pre-compute default focus                           | Return `default_focus` with dominant SP, top type, connected nodes, damage nodes, and suggested scope. Current graph payload only returns nodes and edges.                                                                                                                                                       |           |                                                                                                                                                                                |
| Phase 2 — Investigation layout      | `web/index.html`                                         | Left rail                 | Reorganize controls into collapsible groups         | Replace flat panels with sections: Run Selector, Time Window, Trace Filters, Actions. Current left rail mixes filters, SQL source stats, sync button, legend, and shapes.                                                                                                                                        |           |                                                                                                                                                                                |
| Phase 2 — Investigation layout      | `web/index.html`                                         | Bottom area               | Replace tab strip with diagnostic drawer shell      | Replace always-visible tabs with drawer header and drawer modes: Current Selection, Timeline, Evidence, Raw. Current bottom strip has eight tabs and many empty panels.                                                                                                                                          |           |                                                                                                                                                                                |
| Phase 2 — Investigation layout      | `web/index.html`                                         | Damage entry point        | Promote “What is wrong?”                            | Move `What is wrong?` into top-level action near Open Damage count; keep left-rail copy optional. Current button is inside filter panel.                                                                                                                                                                         |           |                                                                                                                                                                                |
| Phase 2 — Investigation layout      | `web/app.js`                                             | Drawer state              | Add drawer state machine                            | Add `drawerMode`, `drawerCollapsed`, `activeEvidenceId`, and `activeDiagnosisId`; replace simple tab activation with explicit drawer rendering. Current `setupTabs()` only toggles active tab classes.                                                                                                           |           |                                                                                                                                                                                |
| Phase 2 — Investigation layout      | `web/app.js`                                             | Damage mode               | Make wrong mode actionable                          | Change `wrongMode` from pure graph filter into diagnosis mode: show damage list, focus first damage item, hide healthy nodes, and keep drawer open on Damage. Current wrong-mode toggle only flips state and calls `renderGraph()`.                                                                              |           |                                                                                                                                                                                |
| Phase 2 — Investigation layout      | `web/app.js`                                             | `renderAnalytics()`       | Convert analytics tables into diagnosis cards       | Replace raw table-first damage board with grouped diagnosis cards: incomplete runs, gaps, broken instrumentation, slow gaps, rare paths, anonymous traces. Current analytics renderer writes separate tables into tabs.                                                                                          |           |                                                                                                                                                                                |
| Phase 2 — Investigation layout      | `web/app.js`                                             | Diagnosis click handling  | Add graph focus from diagnosis                      | Each diagnosis row/card should call `focusDiagnosis(anomaly)` to set focused node, graph scope, selected drawer evidence, and graph highlight. Current analytics rows are non-interactive tables.                                                                                                                |           |                                                                                                                                                                                |
| Phase 2 — Investigation layout      | `web/styles.css`                                         | Bottom drawer             | Add collapsible drawer styling                      | Add `.diagnostic-drawer`, `.drawer-header`, `.drawer-body`, `.drawer-collapsed`, `.drawer-card`, and `.drawer-mode-button`. Current bottom layout is fixed `bottom-strip` with `tab-bar`.                                                                                                                        |           |                                                                                                                                                                                |
| Phase 2 — Investigation layout      | `web/styles.css`                                         | Left rail                 | Add section accordion styling                       | Add collapsible rail section headers, compact rail metrics, and sticky action group. Current `.panel` is simple grid with border bottom.                                                                                                                                                                         |           |                                                                                                                                                                                |
| Phase 2 — Investigation layout      | `tracer/analyzer.py`                                     | Analytics output          | Add user-facing diagnosis ordering                  | Add `diagnosis` array with normalized fields: `id`, `severity`, `title`, `object_id`, `object_kind`, `reason`, `primary_evidence`, `next_action`, `focus_node_ids`. Current anomalies are generated from metrics but not structured for graph navigation.                                                        |           |                                                                                                                                                                                |
| Phase 2 — Investigation layout      | `server.py`                                              | Analytics endpoint        | Return diagnosis under `/api/analytics`             | Keep existing endpoint but add `diagnosis` to response from `analyzer.analyze()`. Current `/api/analytics` directly returns analyzer output.                                                                                                                                                                     |           |                                                                                                                                                                                |
| Phase 3 — Expected-path model       | `tracer/parsers.py`                                      | `ProcedureInfo` dataclass | Add expected-path fields                            | Extend `ProcedureInfo` with `expected_path`, `parameters`, `error_handlers`, `table_touches_by_step`, `calls_by_step`, and `confidence`. Current dataclass only stores name/found/log_ref_count/calls/reads/inserts/updates/steps/excerpt.                                                                       |           |                                                                                                                                                                                |
| Phase 3 — Expected-path model       | `tracer/parsers.py`                                      | `_extract_steps()`        | Extract richer expected step records                | Add log insert status, source, type, nearby SQL text, `try/catch` context, and probable phase classification. Current step extraction only finds log-table references, string candidates, SrNo, SubSeqNo, ordinal, and step text.                                                                                |           |                                                                                                                                                                                |
| Phase 3 — Expected-path model       | `tracer/parsers.py`                                      | `parse_procedure()`       | Classify expected dependencies near each step       | Instead of procedure-level calls/reads/inserts/updates only, attach nearby table touches and child SP calls to each expected step. Current parse extracts procedure-wide calls/reads/inserts/updates using regex.                                                                                                |           |                                                                                                                                                                                |
| Phase 3 — Expected-path model       | `tracer/parsers.py`                                      | Procedure block parsing   | Preserve parameter definitions                      | Extract parameter names, types, default values, and exec template into `ProcedureInfo`; this becomes expected parameter contract for later parameter delta. Current parser ignores parameter section and reads only definition blocks.                                                                           |           |                                                                                                                                                                                |
| Phase 3 — Expected-path model       | `tracer/cache.py`                                        | Schema                    | Add `expected_paths` table                          | Add table with `procedure_name`, `ordinal`, `sr_no`, `sub_seq_no`, `step_text`, `kind`, `confidence`, `tables_json`, `calls_json`, `error_context_json`, `source_excerpt`. Current DB has only `trace_events`, `procedure_metadata`, `sync_state`, and `anomalies`.                                              |           |                                                                                                                                                                                |
| Phase 3 — Expected-path model       | `tracer/cache.py`                                        | Procedure upsert          | Store expected paths separately                     | Modify `upsert_procedures()` to also upsert parsed expected path rows, not just JSON blobs in `procedure_metadata`. Current function inserts calls/reads/inserts/updates/steps JSON into one metadata table.                                                                                                     |           |                                                                                                                                                                                |
| Phase 3 — Expected-path model       | `tracer/analyzer.py`                                     | Expected-path API builder | Add `expected_path_payload()`                       | Build canonical expected sequence for a procedure from parsed procedure metadata / expected path rows. Include ordered steps, expected calls, table touches, branch info, and confidence. Current playback reads raw `steps_json` only when name filter exists.                                                  |           |                                                                                                                                                                                |
| Phase 3 — Expected-path model       | `tracer/analyzer.py`                                     | Graph payload             | Add expected-path graph mode data                   | Return expected-path nodes/edges separately from full graph so the frontend can render “Expected Path” without actual logs. Current graph payload merges procedures, static steps, table edges, call edges, and unknown trace nodes into one graph.                                                              |           |                                                                                                                                                                                |
| Phase 3 — Expected-path model       | `server.py`                                              | API routing               | Add `/api/expected-path` endpoint                   | Add route accepting `name` and returning `analyzer.expected_path_payload(conn, name)`. Current API exposes workflows, workflow detail, analytics, and playback, but no dedicated expected-path endpoint.                                                                                                         |           |                                                                                                                                                                                |
| Phase 3 — Expected-path model       | `web/index.html`                                         | Graph view mode           | Add Expected Path mode                              | Add option `expected` to `view-mode` or a separate expected/actual toggle. Current view modes are Workflow Map, Step Tower, and Damage Focus.                                                                                                                                                                    |           |                                                                                                                                                                                |
| Phase 3 — Expected-path model       | `web/app.js`                                             | Expected path state       | Store expected path per selected SP                 | Add `expectedPath`, `expectedPathByName`, and `expectedPathStatus`. Load it on selected SP/focused SP change, not only when playback runs. Current app stores `procedures` and `playback`, but not canonical expected path.                                                                                      |           |                                                                                                                                                                                |
| Phase 3 — Expected-path model       | `web/app.js`                                             | Expected path rendering   | Render expected path as ordered rail                | Add `renderExpectedPathGraph()` or branch in `renderGraph()` for expected mode: one SP node, ordered step nodes, table touch side nodes, child SP call nodes, error branch nodes. Current `positionForNode()` has workflow/steps/damage modes only.                                                              |           |                                                                                                                                                                                |
| Phase 4 — Actual-run reconstruction | `tracer/cache.py`                                        | Schema                    | Add `actual_runs` and `actual_run_events` tables    | Add run-level storage: `run_id`, `name`, `type`, `execution_query_hash`, `parameter_signature`, `start_time`, `end_time`, `duration_ms`, `status`, `event_count`, `error_count`; link trace event IDs to runs. Current schema stores trace events but not run grouping.                                          |           |                                                                                                                                                                                |
| Phase 4 — Actual-run reconstruction | `tracer/analyzer.py`                                     | Run grouping              | Add `reconstruct_runs()`                            | Group events by `name`, `type`, `execution_query`, bounded time gaps, and ordered SrNo/SubSeqNo. Current `playback_payload()` just selects rows by filters and sorts them by time/SrNo/SubSeqNo.                                                                                                                 |           |                                                                                                                                                                                |
| Phase 4 — Actual-run reconstruction | `tracer/analyzer.py`                                     | Run classification        | Infer run status                                    | Add status: complete, incomplete, error, unknown, mixed, orphan. Use Entered/Completed/Error step text, error handlers, missing end step, and actual event count. Current analytics detects incomplete runs globally but does not create run objects.                                                            |           |                                                                                                                                                                                |
| Phase 4 — Actual-run reconstruction | `tracer/analyzer.py`                                     | Parameter signature       | Normalize `ExecutionQuery` into run signature       | Hash query + parsed params so repeated runs are grouped consistently without exposing long query strings in selectors. Current parameter parsing exists only inside playback intelligence and only for selected actual events.                                                                                   |           |                                                                                                                                                                                |
| Phase 4 — Actual-run reconstruction | `tracer/sql_client.py`                                   | Live SQL querying         | Add bounded run-window query                        | Add method `events_window(name, type, start, end, limit)` returning all rows in a run window sorted ascending. Current `filtered_events()` returns latest rows sorted descending, useful for tables but not for run reconstruction.                                                                              |           |                                                                                                                                                                                |
| Phase 4 — Actual-run reconstruction | `tracer/sql_client.py`                                   | Live SQL querying         | Add distinct run candidates query                   | Add method returning candidate runs grouped by `Name`, `Type`, `ExecutionQuery`, min/max time, and count. Existing `top_names`, `type_counts`, and `step_counts` summarize but do not identify runs.                                                                                                             |           |                                                                                                                                                                                |
| Phase 4 — Actual-run reconstruction | `server.py`                                              | API routing               | Add `/api/runs` endpoint                            | Return list of reconstructed runs for selected filters. Include name/type/time/status/count/duration/parameter signature. Current route list has `/api/trace/events` and `/api/playback`, but no run selector endpoint.                                                                                          |           |                                                                                                                                                                                |
| Phase 4 — Actual-run reconstruction | `server.py`                                              | API routing               | Add `/api/runs/{run_id}` endpoint                   | Return run detail with ordered events, parsed params, status, error rows, raw event IDs, and linked expected path. Current workflow detail endpoint returns procedure metadata plus recent events, not a specific run.                                                                                           |           |                                                                                                                                                                                |
| Phase 4 — Actual-run reconstruction | `web/index.html`                                         | Left rail                 | Add run selector                                    | Add `Run` dropdown under SP/type/time source; options should show status, start time, duration, event count. Current filters select SP/type/source/time/SrNo/SubSeqNo/search but not run.                                                                                                                        |           |                                                                                                                                                                                |
| Phase 4 — Actual-run reconstruction | `web/app.js`                                             | Filters/state             | Add selected run state                              | Add `runs`, `selectedRunId`, `runEvents`, `runStatus`; update filters to load run candidates after name/type/time change. Current `filters` has name/type/source/limit/start/end/SrNo/SubSeqNo/step/q only.                                                                                                      |           |                                                                                                                                                                                |
| Phase 4 — Actual-run reconstruction | `web/app.js`                                             | Event rendering           | Separate raw events from run events                 | Keep Raw Trace as general result table, but drive playback/actual path from `runEvents`. Current `renderEvents()` displays whatever latest filtered events are loaded.                                                                                                                                           |           |                                                                                                                                                                                |
| Phase 5 — Delta engine              | `tracer/analyzer.py`                                     | Alignment engine          | Add `align_expected_actual()`                       | Implement layered matching: exact SrNo/SubSeqNo, normalized step text similarity, ordinal proximity, procedure-local sequence, time adjacency. Current missing/unexpected logic only uses key sets or loose text containment.                                                                                    |           |                                                                                                                                                                                |
| Phase 5 — Delta engine              | `tracer/analyzer.py`                                     | Delta types               | Add formal delta records                            | Emit `delta_id`, `type`, `severity`, `expected_step`, `actual_event`, `match_method`, `confidence`, `reason`, `impact`, `evidence_ids`, `graph_node_ids`. Current playback only returns `missing_expected` and `unexpected_actual` arrays plus counts.                                                           |           |                                                                                                                                                                                |
| Phase 5 — Delta engine              | `tracer/analyzer.py`                                     | Slow gap logic            | Move slow-gap result into run delta                 | Current slow-gap metric is global and uses `LAG` over name/type/execution_query with threshold >5 seconds. Convert it into per-run deltas linked to specific event pairs.                                                                                                                                        |           |                                                                                                                                                                                |
| Phase 5 — Delta engine              | `tracer/analyzer.py`                                     | Incomplete run logic      | Move incomplete-run result into run delta           | Current incomplete run logic groups by name/type/execution_query and checks Entered > Completed. Convert to run-level delta with evidence event IDs and matched expected completion step.                                                                                                                        |           |                                                                                                                                                                                |
| Phase 5 — Delta engine              | `tracer/cache.py`                                        | Schema                    | Add `run_deltas` table                              | Add persistent table with run_id, delta_type, severity, expected_key, actual_event_id, reason, evidence_json, created_at. Existing anomalies table is global and not tied to run-level expected/actual alignment.                                                                                                |           |                                                                                                                                                                                |
| Phase 5 — Delta engine              | `server.py`                                              | API routing               | Add `/api/delta` endpoint                           | Accept `run_id` or filters and return expected path, actual run, aligned pairs, deltas, summary counts, graph focus nodes. Current `/api/playback` returns expected/actual/unexpected/missing but not alignment records.                                                                                         |           |                                                                                                                                                                                |
| Phase 5 — Delta engine              | `web/app.js`                                             | Damage mode               | Feed “What is wrong?” from delta engine             | Replace damage mode’s reliance on generic `state.analytics.anomalies` with run delta summary when a run is selected; fallback to analytics only when no run is selected. Current wrong mode uses anomaly names to filter graph.                                                                                  |           |                                                                                                                                                                                |
| Phase 5 — Delta engine              | `web/app.js`                                             | Graph rendering           | Overlay delta state on nodes/edges                  | Add `deltaStatusForNode()` and `deltaStatusForEdge()` to color/hollow/pulse nodes: missing, unexpected, out-of-order, duplicate, slow, error, incomplete. Current `colorFor()` only uses selected, filter focus, error, unknown, workflow, step, table.                                                          |           |                                                                                                                                                                                |
| Phase 5 — Delta engine              | `web/styles.css`                                         | Delta visual language     | Add delta classes                                   | Add `.delta-missing`, `.delta-unexpected`, `.delta-slow`, `.delta-incomplete`, `.delta-error`, `.delta-ok`, plus badges/cards. Current CSS only has severity high/medium/low classes.                                                                                                                            |           |                                                                                                                                                                                |
| Phase 5 — Delta engine              | `web/index.html`                                         | Drawer                    | Add Delta drawer panel                              | Add Current Delta Summary as first drawer mode when a run is selected: counts, highest severity, missing steps, unexpected steps, slow steps. Current bottom area has Damage Board and Playback as separate tabs.                                                                                                |           |                                                                                                                                                                                |
| Phase 6 — Visual playback MVP       | `tracer/analyzer.py`                                     | `playback_payload()`      | Return timeline-ready objects                       | Change output from expected/actual raw arrays to `timeline`: aligned ticks with `t`, `expected`, `actual`, `delta`, `prediction`, `graph_highlights`. Current payload returns raw expected, actual, missing, unexpected, transitions, parameter profiles, and counts.                                            |           |                                                                                                                                                                                |
| Phase 6 — Visual playback MVP       | `tracer/analyzer.py`                                     | Time normalization        | Add playback clock                                  | Compute normalized playback positions: actual timestamp range, expected ordinal range, step durations, event gaps, and missing-step insert points. Current actual rows are ordered but not converted into playback ticks.                                                                                        |           |                                                                                                                                                                                |
| Phase 6 — Visual playback MVP       | `server.py`                                              | Playback endpoint         | Accept `run_id`, `mode`, and `speed_context`        | Extend `/api/playback` params to include `run_id`, `playback_mode`, and `include_predictions`; current endpoint only accepts name/type/q/limit/start/end/SrNo/SubSeqNo/step.                                                                                                                                     |           |                                                                                                                                                                                |
| Phase 6 — Visual playback MVP       | `web/index.html`                                         | Playback controls         | Add timeline controls                               | Add play/pause, scrubber, speed selector, previous/next event, current timestamp, current delta badge, playback mode selector. Current graph controls only have a Playback button, no playback strip.                                                                                                            |           |                                                                                                                                                                                |
| Phase 6 — Visual playback MVP       | `web/index.html`                                         | Bottom drawer             | Add timeline rail container                         | Add `<div id="timeline-rail">` with expected, actual, predicted lanes and evidence detail area. Current playback output goes into `tab-playback`.                                                                                                                                                                |           |                                                                                                                                                                                |
| Phase 6 — Visual playback MVP       | `web/app.js`                                             | Playback state            | Add playback runtime state                          | Add `playbackClock`, `playbackIndex`, `playbackSpeed`, `playbackPlaying`, `playbackMode`, `activeTimelineItem`. Current `state.playback` only stores API payload.                                                                                                                                                |           |                                                                                                                                                                                |
| Phase 6 — Visual playback MVP       | `web/app.js`                                             | `runPlayback()`           | Load selected run playback                          | Modify to use `selectedRunId` first, fall back to filters, and request timeline-ready payload. Current `runPlayback()` builds query from filters only and opens playback tab.                                                                                                                                    |           |                                                                                                                                                                                |
| Phase 6 — Visual playback MVP       | `web/app.js`                                             | `renderPlayback()`        | Replace table with visual timeline                  | Replace current expected/actual row table plus JSON prediction model with three-lane visual timeline: Expected, Actual, Predicted. Current renderer builds rows and inserts an HTML table plus JSON block.                                                                                                       |           |                                                                                                                                                                                |
| Phase 6 — Visual playback MVP       | `web/app.js`                                             | Animation loop            | Synchronize graph with playback clock               | Extend `animate()` or add timer to update active node/edge/material per playback tick. Current `animate()` only renders scene every frame.                                                                                                                                                                       |           |                                                                                                                                                                                |
| Phase 6 — Visual playback MVP       | `web/app.js`                                             | Graph playback effects    | Add runtime bead and active edge                    | Add small moving sphere/bead on active actual edge, ghost expected node for pending step, hollow missing node, pulsing unexpected actual node. Current graph has static meshes and static lines only.                                                                                                            |           |                                                                                                                                                                                |
| Phase 6 — Visual playback MVP       | `web/styles.css`                                         | Timeline                  | Add timeline lane styles                            | Add `.timeline`, `.timeline-lane`, `.timeline-item`, `.timeline-item.missing`, `.timeline-item.unexpected`, `.timeline-cursor`, `.playback-controls`. Current CSS only supports tables, heat cells, and compact items.                                                                                           |           |                                                                                                                                                                                |
| Phase 7 — Prediction intelligence   | `tracer/analyzer.py`                                     | Transition model          | Upgrade `learned_transitions()`                     | Return current-step predictions with probability, support count, alternatives, and confidence. Current function returns raw node/next_node/count grouped from historical trace events.                                                                                                                           |           |                                                                                                                                                                                |
| Phase 7 — Prediction intelligence   | `tracer/analyzer.py`                                     | Parameter model           | Upgrade `parameter_intelligence()`                  | Track common keys, missing keys, extra keys, null/empty values, GUID/non-GUID shape, value length, enum rarity, per-SP/type baselines, and anomaly score. Current function only counts common params and flags missing/extra keys.                                                                               |           |                                                                                                                                                                                |
| Phase 7 — Prediction intelligence   | `tracer/analyzer.py`                                     | Timing model              | Add timing baseline function                        | Compute median, p90, p95 gaps between step pairs per SP/type/run signature. Feed into slow-step deltas and prediction confidence. Current timing check only identifies gaps above 5 seconds globally.                                                                                                            |           |                                                                                                                                                                                |
| Phase 7 — Prediction intelligence   | `tracer/analyzer.py`                                     | Completion model          | Add completion profile                              | Learn common completed-run shape: required steps, optional steps, error-only branches, common end states. Use to predict “run likely stopped early.” Current completion logic only compares Entered and Completed counts.                                                                                        |           |                                                                                                                                                                                |
| Phase 7 — Prediction intelligence   | `tracer/cache.py`                                        | Schema                    | Add prediction baseline tables                      | Add `transition_baselines`, `parameter_baselines`, `timing_baselines`, `completion_baselines` keyed by name/type/signature. Current cache has no model/baseline storage.                                                                                                                                         |           |                                                                                                                                                                                |
| Phase 7 — Prediction intelligence   | `server.py`                                              | API routing               | Add `/api/prediction` endpoint                      | Return likely next step, confidence, alternatives, param anomalies, timing anomalies, completion risk. Current prediction-related data is embedded inside playback response only.                                                                                                                                |           |                                                                                                                                                                                |
| Phase 7 — Prediction intelligence   | `web/app.js`                                             | Playback rendering        | Separate static expected from learned prediction    | Display expected path and predicted path as distinct lanes and colors. Current playback section labels transitions and parameter profiles under “Prediction model” JSON.                                                                                                                                         |           |                                                                                                                                                                                |
| Phase 7 — Prediction intelligence   | `web/app.js`                                             | Inspector                 | Add “Why predicted?” detail                         | Clicking a predicted node/edge should show support count, probability, historical examples, parameter/timing evidence. Current inspector only handles node kind and workflow details.                                                                                                                            |           |                                                                                                                                                                                |
| Phase 7 — Prediction intelligence   | `web/styles.css`                                         | Prediction visuals        | Add predicted/ghost styling                         | Add translucent predicted lane, ghost nodes, confidence badges, probability bars, and anomaly score chips. Current visual status classes only cover severity.                                                                                                                                                    |           |                                                                                                                                                                                |
| Phase 8 — Evidence-first inspection | `web/app.js`                                             | Raycasting                | Add edge selection                                  | Maintain `edgeMeshes`, raycast against edges or invisible pick tubes, and call `inspectEdge(edge)`. Current raycast only intersects `nodeMeshes`.                                                                                                                                                                |           |                                                                                                                                                                                |
| Phase 8 — Evidence-first inspection | `web/app.js`                                             | Inspector rendering       | Replace JSON dumps with evidence cards              | Replace `JSON.stringify()` evidence with structured sections: Meaning, Shape, Static evidence, Actual evidence, Delta evidence, Raw. Current inspector writes raw JSON to `inspect-evidence`.                                                                                                                    |           |                                                                                                                                                                                |
| Phase 8 — Evidence-first inspection | `web/app.js`                                             | Table node inspector      | Add schema/table metadata section                   | For table nodes, show table role, related SPs, row count, date range, columns, top evidence events, and write/update/read context. Current table inspector shows meaning, table, roles, related edges, related procedures only.                                                                                  |           |                                                                                                                                                                                |
| Phase 8 — Evidence-first inspection | `web/app.js`                                             | Step node inspector       | Add expected/actual/delta alignment                 | For step nodes, show expected step, matched actual event, delta status, timing, raw trace rows, and source SP excerpt. Current step inspector shows selected static step and matching visible events.                                                                                                            |           |                                                                                                                                                                                |
| Phase 8 — Evidence-first inspection | `web/app.js`                                             | SP node inspector         | Add run/evidence summary                            | For SP nodes, show expected path summary, actual runs, status distribution, child calls, table reads/writes/updates, error branch count, and recent selected-run evidence. Current SP inspector fetches workflow detail and shows procedure JSON plus recent events.                                             |           |                                                                                                                                                                                |
| Phase 8 — Evidence-first inspection | `web/index.html`                                         | Modal                     | Add modal tabs/sections                             | Replace single `<pre id="inspect-evidence">` with tabbed/cards layout: Summary, Expected, Actual, Delta, Raw. Current modal has metadata and one Evidence pre block.                                                                                                                                             |           |                                                                                                                                                                                |
| Phase 8 — Evidence-first inspection | `web/styles.css`                                         | Evidence cards            | Add structured evidence CSS                         | Add `.evidence-card`, `.evidence-section`, `.evidence-grid`, `.raw-toggle`, `.object-type-chip`, `.edge-type-chip`, `.confidence-chip`. Current modal/pre styling is optimized for raw text.                                                                                                                     |           |                                                                                                                                                                                |
| Phase 8 — Evidence-first inspection | `tracer/parsers.py`                                      | Schema parsing            | Expand `parse_schema_row_counts()`                  | Add column definitions, primary keys, date ranges, row counts, and table summaries from schema Markdown. Current parser only extracts row counts from summary-like lines.                                                                                                                                        |           |                                                                                                                                                                                |
| Phase 8 — Evidence-first inspection | `tracer/cache.py`                                        | Schema cache              | Add `table_metadata` table                          | Store table name, row count, date range, primary key, columns_json, source_doc timestamp. Current cache does not persist schema metadata.                                                                                                                                                                        |           |                                                                                                                                                                                |
| Phase 8 — Evidence-first inspection | `tracer/analyzer.py`                                     | Evidence builder          | Add `evidence_payload()`                            | Given object type/id/run_id/edge_id, return structured evidence cards and raw fallback. This prevents frontend from having to assemble evidence from multiple unrelated payloads. Current evidence is assembled in frontend inspector using node data and workflow detail API.                                   |           |                                                                                                                                                                                |
| Phase 8 — Evidence-first inspection | `server.py`                                              | API routing               | Add `/api/evidence` endpoint                        | Accept `kind`, `id`, `run_id`, `edge_id` and return structured evidence. Current APIs expose workflow detail and events, but no unified evidence endpoint.                                                                                                                                                       |           |                                                                                                                                                                                |
| Phase 9 — Export/reporting          | `web/app.js`                                             | Export PNG                | Add export modes                                    | Expand `exportGraphImage()` to support current viewport, clean graph, expected-vs-actual overlay, playback frame, and include/exclude labels. Current export renders current canvas and downloads PNG.                                                                                                           |           |                                                                                                                                                                                |
| Phase 9 — Export/reporting          | `web/index.html`                                         | Export UI                 | Add export modal                                    | Replace single Export PNG action with export modal: image type, include title, include filters, include legend, include evidence summary, transparent background. Current graph controls have only `Export PNG` button.                                                                                          |           |                                                                                                                                                                                |
| Phase 9 — Export/reporting          | `web/app.js`                                             | Report rendering          | Add client-side report preview                      | Add `renderReportPreview()` for selected run: header, filters, expected/actual timeline, delta list, evidence links, raw export buttons. Current report-like output is only tables and JSON in tabs.                                                                                                             |           |                                                                                                                                                                                |
| Phase 9 — Export/reporting          | `tracer/analyzer.py`                                     | Report payload            | Add `diagnostic_report_payload()`                   | Generate report-ready data: selected run, expected path, actual path, deltas, predictions, evidence summary, filter context, created_at. Current analyzer separates analytics, graph, and playback payloads.                                                                                                     |           |                                                                                                                                                                                |
| Phase 9 — Export/reporting          | `server.py`                                              | API routing               | Add `/api/report` endpoint                          | Return selected run diagnostic report data as JSON. Keep PDF generation out for now unless needed; frontend can render printable HTML. Current routes do not expose report payload.                                                                                                                              |           |                                                                                                                                                                                |
| Phase 9 — Export/reporting          | `server.py`                                              | CSV export                | Add `/api/export/deltas.csv` endpoint               | Return selected run deltas as CSV: type, severity, expected, actual, reason, evidence. Current server only returns JSON.                                                                                                                                                                                         |           |                                                                                                                                                                                |
| Phase 9 — Export/reporting          | `web/styles.css`                                         | Printable/export view     | Add print/report CSS                                | Add `@media print`, `.report-page`, `.report-header`, `.report-section`, `.export-clean`, and hide app chrome during report export. Current CSS has no print/export layout.                                                                                                                                      |           |                                                                                                                                                                                |
| Phase 9 — Export/reporting          | `tracer/cache.py`                                        | Export history            | Add optional `investigation_snapshots` table        | Store selected filter/run/delta/report snapshot so exported evidence can be reproduced. Current cache stores events, procedure metadata, sync state, and anomalies only.                                                                                                                                         |           |                                                                                                                                                                                |
| Phase 10 — Observability            | `server.py`                                              | Runtime logging           | Add request IDs and response metadata               | Extend `log_runtime()` with request_id, response rows, source, cache/sql mode, error text, and endpoint-specific metadata. Current server logs timestamp, method, path, status, elapsed_ms, and query.                                                                                                           |           |                                                                                                                                                                                |
| Phase 10 — Observability            | `server.py`                                              | API response              | Include request ID in every response                | Add `request_id` to JSON payloads and response headers so frontend/runtime log rows can be correlated. Current `json_response()` only serializes payload and writes content headers.                                                                                                                             |           |                                                                                                                                                                                |
| Phase 10 — Observability            | `tracer/sql_client.py`                                   | SQL timing                | Instrument query execution                          | Wrap `_run()` / `query_json()` timing with query label, duration, row count, timeout, sqlcmd status, and error message. Current SQL client runs command and raises error if return code is nonzero.                                                                                                              |           |                                                                                                                                                                                |
| Phase 10 — Observability            | `tracer/sql_client.py`                                   | Query identity            | Add SQL operation names                             | Add optional `label` argument to query methods so runtime logs show `trace_events`, `runs`, `sync`, `summary`, etc., rather than only endpoint path. Current methods do not carry query labels.                                                                                                                  |           |                                                                                                                                                                                |
| Phase 10 — Observability            | `web/app.js`                                             | Browser runtime log       | Add boot-stage events                               | Add explicit runtime marks: boot start, summary loaded, workflows loaded, analytics loaded, graph rendered, filters applied, playback loaded. Current `api()` logs only browser fetch status/latency.                                                                                                            |           |                                                                                                                                                                                |
| Phase 10 — Observability            | `web/app.js`                                             | Runtime UI                | Make runtime panel chronological and filterable     | Add filters for browser/server/sql/render/error, request ID linking, row counts, and render duration. Current runtime table shows ts, layer, path, status, elapsed_ms, source.                                                                                                                                   |           |                                                                                                                                                                                |
| Phase 10 — Observability            | `web/app.js`                                             | Runtime modal             | Replace raw JSON log dump                           | Render runtime as structured timeline with request groups and expandable raw rows. Current modal fetches `/api/runtime/logs` and dumps JSON into `inspect-evidence`.                                                                                                                                             |           |                                                                                                                                                                                |
| Phase 10 — Observability            | `web/index.html`                                         | Runtime controls          | Add runtime toggle/filter controls                  | Add runtime overlay controls: show/hide, errors only, last refresh, request ID search. Current UI only has `Runtime log` button and runtime tab.                                                                                                                                                                 |           |                                                                                                                                                                                |
| Phase 10 — Observability            | `web/styles.css`                                         | Runtime display           | Add runtime timeline styles                         | Add `.runtime-timeline`, `.runtime-row`, `.runtime-error`, `.runtime-sql`, `.runtime-browser`, `.runtime-server`, `.request-group`. Current table styling is generic.                                                                                                                                            |           |                                                                                                                                                                                |
| Phase 10 — Observability            | `tracer/cache.py`                                        | Observability persistence | Add optional `runtime_events` table                 | Persist last N server/sql/runtime records across refreshes if needed. Current runtime log is an in-memory deque in `server.py`; cache has no runtime table.                                                                                                                                                      |           |                                                                                                                                                                                |
| Cross-phase                         | `tracer/config.py`                                       | Runtime constants         | Add feature flags and thresholds                    | Add constants/env vars for focused graph node limit, max graph nodes, run gap threshold, slow gap threshold, playback max events, prediction min support, and observability enabled. Current config only contains paths, trace view/database, and SQL config.                                                    |           |                                                                                                                                                                                |
| Cross-phase                         | `tracer/__init__.py`                                     | Package exports           | Keep minimal unless modules split                   | If new modules are created, optionally export package version or module names; otherwise no functional change needed. Current file is only package marker.                                                                                                                                                       |           |                                                                                                                                                                                |
| Cross-phase                         | `web/vendor/three.module.js`                             | Vendored dependency       | Do not edit                                         | Keep Three.js vendored file untouched; all graph behavior should be implemented in `app.js` using Three APIs, not by modifying the library. Current app imports Three from this file.                                                                                                                            |           |                                                                                                                                                                                |
| Cross-phase                         | `Xstudio_Xbatch_StoredProcedures_2026-05-22_09-18-05.md` | Static source document    | Do not edit; parse richer information from it       | Use it as source for expected path, parameters, calls, reads, writes, and error branches. Parser currently reads this document via `SP_DOC_PATH`.                                                                                                                                                                |           |                                                                                                                                                                                |
| Cross-phase                         | `Xstudio_Xbatch_Schema_2026-05-22_09-15-29.md`           | Static source document    | Do not edit; parse table metadata from it           | Use it as source for table row counts, date ranges, primary keys, and columns for table inspectors. Config already points to this schema document via `SCHEMA_DOC_PATH`.                                                                                                                                         |           |                                                                                                                                                                                |
| Cross-phase                         | `Log View Sample Data.xlsx`                              | Sample-only data          | Do not use as runtime source                        | Keep sample spreadsheet out of runtime path. Runtime source should remain SQL view/cache; current config points runtime to `dbo.XStudio_List_XMES_Log_Trn_Tbl_Vw`.                                                                                                                                               |           |                                                                                                                                                                                |
